<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="AppTheme.Launcher">
    <item name="android:windowBackground">@layout/splash_layout</item>
    </style>

</resources>
